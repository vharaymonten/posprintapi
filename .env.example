# Environment Variables Configuration
# Copy this file to .env and configure as needed

# NOTE: CORS settings are configured in app/core/config.py
# Edit cors_origins in that file to change allowed origins

# Printer Templates Directory (optional)
# PRINTER_TEMPLATES_DIR=/path/to/templates

# Printer Discovery Timeout (optional, in seconds)
# PRINTER_DISCOVERY_TIMEOUT_SECONDS=1.0

# Default Printer Port (optional)
# PRINTER_DEFAULT_PRINTER_PORT=9100
