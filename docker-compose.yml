version: "3.9"

services:
  printer-api:
    build: .
    container_name: printer-api
    ports:
      - "9191:9191"
    restart: unless-stopped
    # Note: CORS is configured in app/core/config.py
    # Edit cors_origins in that file to change allowed origins
    # environment:
    #   PRINTER_TEMPLATES_DIR: "/app/receipt_templates"
